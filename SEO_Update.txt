Role: You are an Expert SEO Specialist and Senior Frontend Engineer. 

Objective: Optimize the existing production website for maximum search engine visibility, accessibility (a11y), and semantic structure. 

CRITICAL CONSTRAINT: You MUST NOT change any of the existing visible website text, copy, layout, or design. Your modifications must be strictly limited to HTML attributes, the `<head>` section, configuration files, and semantic tag replacements. 

The website context focuses on the Cares Circular Economy initiatives, including green energy, waste management, services, and publications.

Please execute the following tasks systematically:

1. Metadata & Head Tags (Searchability & Social Sharing)
- Add comprehensive SEO meta tags to all pages (Title, Description, Keywords). Ensure titles and descriptions are optimized for search engines to easily index names, services, and publications.
- Add Open Graph (OG) tags and Twitter Cards for rich social media sharing.
- Ensure the viewport meta tag is correctly set for mobile responsiveness.
- Define a canonical URL tag for the pages to prevent duplicate content issues.

2. Favicon Replacement
- Remove all default/placeholder "Lovable UI" favicons.
- Set up the standard `<link rel="icon">` tags pointing to the correct project logo/favicon assets (include standard .ico, .png sizes, and apple-touch-icon). 
- i put the real logo on this dir = public\assets\CARES_LOGO.png use thsi i alredy chage this but if any thing reamain related to lovable stil show the same fix that also

3. Accessibility & Screen Reader Optimization (Disability Compliance)
- Audit all `<img>` tags and add highly descriptive, keyword-rich `alt` attributes. Do not use generic terms like "image" or "picture".
- Ensure proper semantic HTML is used (e.g., `<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`) instead of generic `<div>` tags where appropriate.
- Add `aria-labels` to buttons, links, or interactive elements that lack clear text content (e.g., icon-only buttons).

4. Search Engine Crawling (Robots.txt & Sitemap)
- Create a `robots.txt` file at the root level that allows all standard search engine bots (User-agent: *) to crawl the site, while restricting access to any private/admin directories if they exist.
- Include a link to the `sitemap.xml` inside the `robots.txt` file.
- If a `sitemap.xml` does not exist, generate a script or file structure mapping out the key pages (Home, Services, Publications, Contact, etc.).

5. Heading Structure (Ranking Optimization)
- Review the heading tags (H1, H2, H3) on all pages. Ensure there is only one exactly matching H1 per page, representing the primary topic. 
- Ensure H2 and H3 tags logically break down the services and publications without altering the actual text on the screen.

Output Requirement: Provide the exact file changes, configuration files (robots.txt), and code snippets needed to implement these updates, and put all project frontend page folder stracutre in this file also dont forgot that